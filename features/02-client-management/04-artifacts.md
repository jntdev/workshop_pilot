# 04 - Artifacts attendus

- Backend : migration `create_clients_table`, modèle `Client`, contrôleur API, FormRequest/DTO, test Feature `CreateClientTest`.
- Frontend (Livewire) : composant `App\Livewire\Clients\Form`, vue Blade associée, test Livewire `Clients\FormTest`.
- Documentation : fichiers de cette feature + README dédiés si besoin. 
